百度提前批笔试
选择-代码缩进python基础 2题

天空-python不常用函数
zip、lambda字典排序、正则表达式%s、两个变量同一行赋值

问答：
1、大文件1000T排序
2、数据集分割：训练集与测试集不交、训练集是1/n  2/N......n-1/n
第二题我是先把数据装到不同list中再处理，后来想这样不行，如果数据非常大会爆内存
所以比较好的方式是用随机函数，在不同的概率区间采取不同的行为。



0825深信服：
选择：
删除稀疏特征是否可以缓解过拟合？
召回率为0.99，误报率为0.01，正负比例：1:100，求精确率

编程：
1、用变长为1隔板进行区间划分，要求尽量紧凑不能有空缺，
不保证提供的板子可以最终拼成矩形，给n个隔板，求能划分
处多少房间？

2、破解密码
第一行输入n, m, k
第二行是一个包含n个数据的数组a
接下来m行数据吗，每个数据代表边界值l，r
如果有i, j 满足 l<=i<j<=k,a[i]*a[j]+a[i]+a[j]=k，则count+1(count初始化为0)求count
暴搜过了0.4，应该需要针对每行数据做优化

3、最合适的时间
n行数据表示每个人可以旅行的时间边界
求在在哪个时间旅行，能去的人最多？
